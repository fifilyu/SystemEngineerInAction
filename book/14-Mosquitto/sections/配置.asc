=== 配置

----
/etc/mosquitto/
├── aclfile.example
├── mosquitto.conf
├── pskfile.example
└── pwfile.example
----

创建用户：

[source, bash]
useradd -c "mosquitto server user" -d /var/lib/mosquitto -s /sbin/nologin  mosquitto

